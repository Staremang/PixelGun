function isMobile() {
    return $(window).width() <= 768;
}

function videoResize() {
    var e = $(window).height(), o = $(window).width(), n = document.getElementById("video");
    o / e >= 16 / 9 ? (player.setSize(o, o / (16 / 9)), n.style.marginTop = (e - o / (16 / 9)) / 2 + "px", 
    n.style.marginLeft = "") : (player.setSize(e * (16 / 9), e), n.style.marginLeft = (o - e * (16 / 9)) / 2 + "px", 
    n.style.marginTop = "");
}

function onYouTubeIframeAPIReady() {
    var e = 0, o = 0, n = $(window).height(), t = $(window).width();
    t / n >= 16 / 9 ? (o = t, e = t / (16 / 9)) : (e = n, o = n * (16 / 9)), player = new YT.Player("video", {
        height: e,
        width: o,
        videoId: "CRhJLTA-Xpk",
        playerVars: {
            autoplay: 1,
            controls: 0,
            showinfo: 0,
            loop: 1,
            rel: 0,
            playlist: "CRhJLTA-Xpk"
        },
        events: {
            onReady: onPlayerReady
        }
    }), $(window).resize(function() {
        videoResize();
    });
}

function onPlayerReady(e) {
    e.target.playVideo(), e.target.mute(), videoResize(), pausePlayVideo(), player.addEventListener("onStateChange", function(e) {
        1 == e.data && $(".video-cup").fadeOut();
    });
}

function pausePlayVideo() {
    $("body").hasClass("disabled-onepage-scroll") ? $(window).scrollTop() > $(window).height() + 50 ? player.pauseVideo() : player.playVideo() : $("body").hasClass("viewing-page-1") ? player.playVideo() : player.pauseVideo(), 
    $(".wrapper").onepage_scroll({
        updateURL: !0,
        loop: !1,
        beforeMove: function(e) {
            1 == e && done && (player.playVideo(), done = !1);
        },
        afterMove: function(e) {
            1 === e || done || (player.pauseVideo(), done = !0);
        }
    });
}

var player, done = !1;

$(document).ready(function() {
    var e = document.createElement("script");
    e.src = "https://www.youtube.com/iframe_api";
    var o = document.getElementsByTagName("script")[0];
    o.parentNode.insertBefore(e, o), document.querySelector(".header-navigation__btn").addEventListener("click", function() {
        document.querySelector(".header-container").classList.toggle("view");
    }), document.querySelector(".header-display__btn").addEventListener("click", function() {
        document.querySelector(".header-container").classList.toggle("view");
    }), $(".wrapper").onepage_scroll({
        sectionContainer: "section",
        updateURL: !0,
        loop: !1
    }), $(".section-hero-mouse-icon").click(function() {
        $(".wrapper").moveDown();
    }), $(".screenshots-slider").owlCarousel({
        loop: !0,
        nav: !1,
        items: 1,
        center: !0
    });
});