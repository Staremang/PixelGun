function isMobile() {
    return document.documentElement.clientWidth <= 575;
}

function fadeOut(e) {
    e.style.opacity = 1;
    var o = +new Date(), t = function() {
        e.style.opacity = +e.style.opacity - (new Date() - o) / 400, o = +new Date(), +e.style.opacity > 0 && (window.requestAnimationFrame && requestAnimationFrame(t) || setTimeout(t, 16));
    };
    t();
}

function ready(e) {
    (document.attachEvent ? "complete" === document.readyState : "loading" !== document.readyState) ? e() : document.addEventListener("DOMContentLoaded", e);
}

function onYouTubeIframeAPIReady() {
    var e = 0, o = 0, t = document.documentElement.clientHeight, n = document.documentElement.clientWidth;
    n / t >= 16 / 9 ? (o = n, e = n / (16 / 9)) : (e = t, o = t * (16 / 9)), player = new YT.Player("video", {
        height: e,
        width: o,
        videoId: videoId,
        playerVars: {
            autoplay: 1,
            controls: 0,
            showinfo: 0,
            loop: 1,
            rel: 0,
            playlist: videoId
        },
        events: {
            onReady: onPlayerReady
        }
    }), window.addEventListener("resize", function() {
        videoResize();
    });
}

function onPlayerReady(e) {
    videoResize(), e.target.playVideo(), e.target.mute(), pausePlayVideo(), player.addEventListener("onStateChange", function(e) {
        1 == e.data && fadeOut(document.querySelector(".video-cup"));
    });
}

function videoResize() {
    var e = document.documentElement.clientHeight, o = document.documentElement.clientWidth, t = document.getElementById("video");
    o / e >= 16 / 9 ? (player.setSize(o, o / (16 / 9)), t.style.marginTop = (e - o / (16 / 9)) / 2 + "px", 
    t.style.marginLeft = "") : (player.setSize(e * (16 / 9), e), t.style.marginLeft = (o - e * (16 / 9)) / 2 + "px", 
    t.style.marginTop = "");
}

function pausePlayVideo() {
    document.querySelector("body").classList.contains("disabled-onepage-scroll") || !document.querySelector("body").classList.contains("onepage") ? window.addEventListener("scroll", function() {
        window.pageYOffset > document.documentElement.clientHeight + 50 ? player.pauseVideo() : player.playVideo();
    }) : document.querySelector("body").classList.contains("viewing-page-1") ? player.playVideo() : player.pauseVideo(), 
    beforePage = function(e) {
        1 == e && done && (player.playVideo(), console.log("play"), done = !1);
    }, afterPage = function(e) {
        1 === e || done || (player.pauseVideo(), console.log("pause"), done = !0);
    };
}

function initMap() {
    map = new google.maps.Map(document.getElementById("map"), {
        center: mapCenter,
        zoom: 8
    });
}

function openCloseMenu() {
    document.querySelector(".header-container").classList.toggle("view");
}

var player, videoId = "CRhJLTA-Xpk", done = !1, map, mapApiKey = "AIzaSyAfunSVDqMzVk4Bb5yplXxK9f2DnWXcWFk", mapCenter = {
    lat: -34.397,
    lng: 150.644
}, beforePage = function() {}, afterPage = function() {};

ready(function() {
    var e = document.getElementsByTagName("script")[0], o = document.createElement("script");
    if (o.src = "https://maps.googleapis.com/maps/api/js?key=" + mapApiKey + "&callback=initMap", 
    e.parentNode.insertBefore(o, e), !isMobile()) {
        var t = document.createElement("script");
        t.src = "https://www.youtube.com/iframe_api", e.parentNode.insertBefore(t, e);
    }
    document.querySelector(".header-navigation__btn").addEventListener("click", function() {
        openCloseMenu();
    }), document.querySelector(".header-display__btn").addEventListener("click", function() {
        openCloseMenu();
    });
}), $(document).ready(function() {
    function e(e, o) {
        var t = isMobile();
        isMobile() ? ($(e).addClass("owl-carousel"), $(e).addClass("owl-theme"), $(e).owlCarousel(o)) : ($(e).removeClass("owl-carousel"), 
        $(e).removeClass("owl-theme"), $(e).trigger("destroy.owl.carousel")), $(window).resize(function() {
            isMobile() && !t ? (t = !0, $(e).addClass("owl-carousel"), $(e).addClass("owl-theme"), 
            $(e).owlCarousel(o)) : !isMobile() && t && (t = !1, $(e).removeClass("owl-carousel"), 
            $(e).removeClass("owl-theme"), $(e).trigger("destroy.owl.carousel"));
        });
    }
    isMobile() ? $(".header-navigation__item").click(function() {
        openCloseMenu();
    }) : ($("body").addClass("onepage"), $(".wrapper").onepage_scroll({
        sectionContainer: "section",
        updateURL: !0,
        loop: !1,
        beforeMove: function(e) {
            beforePage(e);
        },
        afterMove: function(e) {
            afterPage(e);
        },
        responsiveFallback: 769
    }), $('a[href="#company"]').click(function(e) {
        e.preventDefault(), $(".wrapper").moveTo(2);
    }), $('a[href="#games"]').click(function(e) {
        e.preventDefault(), $(".wrapper").moveTo(3);
    }), $('a[href="#jobs"]').click(function(e) {
        e.preventDefault(), $(".wrapper").moveTo(4);
    }), $('a[href="#contact"]').click(function(e) {
        e.preventDefault(), $(".wrapper").moveTo(5);
    }), $(".section-hero-mouse-icon").click(function() {
        $(".wrapper").moveDown();
    })), $(".screenshots-slider").owlCarousel({
        loop: !0,
        nav: !1,
        autoWidth: !0,
        items: 1,
        center: !0,
        responsive: {
            0: {
                margin: 0
            },
            576: {
                margin: -32
            },
            769: {
                margin: -37
            },
            992: {
                margin: -58
            },
            1240: {
                margin: -80
            }
        }
    }), e(".about-slider", {
        loop: !0,
        nav: !1,
        items: 1
    }), e(".vacancy-container", {
        loop: !0,
        nav: !1,
        items: 1
    });
});